{
  "name": "@servicebus/core",
  "version": "0.0.0-development",
  "description": "core bus that other busses can extend that provides event emitter and middleware functionality",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier-standard 'lib/**/*.js' 'index.js'"
  },
  "lint-staged": {
    "linters": {
      "**/*.js": [
        "prettier-standard",
        "git add"
      ]
    }
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && npm run test"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/servicebus/core.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/servicebus/core/issues"
  },
  "homepage": "https://github.com/servicebus/core#readme",
  "dependencies": {
    "readable-id-mjs": "^1.0.0"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "jest": "^24.7.0",
    "lint-staged": "^8.1.5",
    "prettier-standard": "^9.1.1"
  }
}
